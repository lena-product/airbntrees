<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<%= @user.username %>
<% @user.bookings.each do |booking| %>
  <%= booking.christmastree.name  %>
  <%= link_to "Delete", booking_path(booking), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
    <% end %>

<% #@user_christmastrees.each do |tree| %>
      <p><%#= link_to tree.name, christmastree_path(tree) %></p>
       <%#= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
      <%#= link_to "delete", christmastree_path(tree), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
  <% end %>
  <%# <%= link_to christmastree.destroy %>
