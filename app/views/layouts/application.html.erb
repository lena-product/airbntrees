<!DOCTYPE html>
<html>
  <head>
    <title>Airbntrees</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <style>
      body {
        background-image: linear-gradient(rgba(255,255,255,0.3), rgba(255,255,255,0.3)), url("https://cdn.windowsreport.com/wp-content/uploads/2019/12/Christmas-886x590.jpg");
        background-repeat: no-repeat;
        background-attachment: fixed;
        background-size: cover;
      }
    </style>
  </head>

  <body>
    <%# <div style="background-image: url('https://www.website.com/images/image_background.jpg');"> %>
    <% if params[:controller] != 'pages' && params[:action] != 'home' %>
      <%= render "shared/navbar" %>
    <% end %>
      <div class="wrapper">
    <%= render "shared/flashes" %>
    <%= yield %>
      <div>
        <% if params[:controller] != 'pages' && params[:action] != 'home' %>
          <%= render "shared/footer" %>
        <% end %>
      </div>
    <%# </div> %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
